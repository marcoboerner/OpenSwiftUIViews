import XCTest
@testable import SwiftUIOpenScrollView

final class SwiftUI_AnimationTestTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_AnimationTest().text, "Hello, World!")
    }
}
